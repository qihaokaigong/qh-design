---
"@qhkg/react": minor
---

Add stable per-component JavaScript and CSS subpath exports, retain the existing aggregate entries, and verify on-demand bundle size and tree shaking during release checks.
